---
import config from "@/config/config.json";
import Base from "@/layouts/Base.astro";
import { markdownify } from "@/lib/utils/textConverter";
---

<Base 
  title="Gracias por contactarnos - Ticmans" 
  meta_title="Gracias por tu Mensaje - Ticmans"
  description="Gracias por contactar a Ticmans. Hemos recibido tu mensaje y nos pondremos en contacto contigo pronto."
>
  <section class="section pt-24 xl:pt-40">
    <div class="container">
      <div class="row justify-center">
        <div class="text-center lg:col-10 xl:col-8">
          <h1 class="mb-4" set:html={markdownify("¡Gracias por tu **interés**!")} />
          <p class="mb-12">
            Hemos recibido tu consulta correctamente. Nuestro equipo de expertos en tecnología 
            se pondrá en contacto contigo en las próximas 24 horas para brindarte una propuesta 
            personalizada.
          </p>
          
          <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
            <a href="/" class="btn btn-primary">
              Volver al inicio
            </a>
            <a href="/servicios" class="btn btn-outline-primary">
              Ver Nuestros Servicios
            </a>
          </div>
          
          <div class="text-center text-sm text-text/70">
            <p>
              <strong>¿Necesitas contactarnos urgentemente?</strong><br>
              {config.params.contact_info.map((contact, index) => (
                <>
                  {contact.title}: {contact.label && contact.link ? (
                    <a href={contact.link} target="_blank" class="text-primary hover:underline">
                      {contact.label}
                    </a>
                  ) : contact.content && contact.link ? (
                    <a href={contact.link} target="_blank" class="text-primary hover:underline">
                      {contact.content}
                    </a>
                  ) : (
                    contact.content || contact.location
                  )}
                  {index < config.params.contact_info.length - 1 && " | "}
                </>
              ))}
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>
</Base>
